{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "HOST=0.0.0.0 nuxt dev",
    "dev:full": "concurrently \"npm run dev\" \"npm --prefix server start\"",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "start": "npm --prefix server start",
    "start:cc": "concurrently \"nuxt preview\" \"npm --prefix server start\""
  },
  "dependencies": {
    "@unocss/nuxt": "^66.5.4",
    "nuxt": "^3.13.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "unocss": "^66.5.4"
  }
}


